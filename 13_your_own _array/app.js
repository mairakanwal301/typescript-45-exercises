// exercise:13
var transpotations = ['honda bike', 'car', 'corola', 'truck', 'prado'];
for (var i = 0; i < transpotations.length; i++) {
    console.log("I would like to own " + transpotations[i]);
}
